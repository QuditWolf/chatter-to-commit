# Fleet Pipeline — Python dependencies

# API server
fastapi>=0.110
uvicorn[standard]>=0.28
websockets>=12.0   # WebSocket support for FastAPI
pydantic>=2.0
pytz>=2024.1

openai>=1.0                # OpenAI-compatible client (vllm serve / Ollama / llama.cpp)
python-dotenv>=1.0         # loads .env file into os.environ at startup

# LLM serving (run separately)
# vllm>=0.4                # pip install vllm   (GPU required)
# Start: vllm serve Qwen/Qwen2.5-7B-Instruct-AWQ --port 8001 --tensor-parallel-size 1

# Optional: for richer WhatsApp parsing
# regex>=2023.0
