FROM node:20-slim

WORKDIR /app

COPY fleet_pipeline/wa_listener/package*.json ./
RUN npm install --omit=dev

COPY fleet_pipeline/wa_listener/index.js ./

ENV FLEET_API_URL=http://api:8000 \
    WA_HEALTH_PORT=3001 \
    WA_SESSION_DIR=/session

EXPOSE 3001

CMD ["node", "index.js"]
